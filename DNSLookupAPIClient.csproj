<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.1</TargetFramework>
    <Nullable>enable</Nullable>
    <GeneratePackageOnBuild>True</GeneratePackageOnBuild>
    <Title>APIVerve.API.DNSLookup</Title>
    <Version>1.0.0</Version>
    <Authors>APIVerve</Authors>
    <Company>APIVerve</Company>
    <PackageLicenseFile>LICENSE</PackageLicenseFile>
    <PackageRequireLicenseAcceptance>False</PackageRequireLicenseAcceptance>
    <PackageReadmeFile>Readme.md</PackageReadmeFile>
    <PackageProjectUrl>https://apiverve.com/marketplace/api/dnslookup</PackageProjectUrl>
    <SignAssembly>False</SignAssembly>
    <Description>DNS Lookup is a simple tool for looking up the DNS records of a domain. It returns the A, MX, and other records of the domain.</Description>
    <RepositoryUrl>https://github.com/apiverve/dnslookup-NET</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageTags>dns; dns lookup; dns records; dns lookup api; dns records api; dns lookup tool; dns records tool; dns lookup software; dns records software; dns lookup service; dns records service; dns lookup library; dns records library; dns lookup sdk; dns records sdk; dns lookup code; dns records code; dns lookup script; dns records script; dns lookup program; dns records program; dns lookup application; dns records application; dns lookup app; dns records app; dns lookup tool; dns records tool; dns lookup utility; dns records utility; dns lookup helper; dns records helper; dns lookup helper tool; dns records helper tool; dns lookup helper software; dns records helper software; dns lookup helper service; dns records helper service; dns lookup helper library; dns records helper library; dns lookup helper sdk; dns records helper sdk; dns lookup helper code; dns records helper code; dns lookup helper script; dns records helper script; dns lookup helper program; dns records helper program; dns lookup helper application; dns records helper application; dns lookup helper app; dns records helper app; dns lookup helper tool; dns records helper tool; dns lookup helper utility; dns records helper utility; dns lookup helper; dns records helper</PackageTags>
    <Copyright>©2024 APIVerve. All Rights Reserved</Copyright>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Newtonsoft.Json" Version="13.0.3" />
  </ItemGroup>

  <ItemGroup>
    <None Update="LICENSE">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
    <None Update="Readme.md">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
  </ItemGroup>

</Project>
